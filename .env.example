# Copy to `.env` at project root. Do not commit `.env`.

# Stripe publishable key only (Dashboard → Developers → API keys)
STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key_here

# Payment server URL is set in lib/app/core/config/app_config.dart
# Secret keys (sk_...) belong on the server only, never in this file.
